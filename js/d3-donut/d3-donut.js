import { Init, d3Remove, sumTotal, makecomma, d3, svgCheck, log } from '../helper/d3-helper';
import CLASS from '../helper/d3-selector';

const tag = ['DonutChart'];

const DonutChart = function(...arg) {
  if(window.console) log(arg)
}
export default DonutChart;
